var documenterSearchIndex = {"docs":
[{"category":"section","location":"#Taxsim.jl","page":"Home","text":"Julia interface to the NBER's TAXSIM calculator for US federal and state income tax liabilities.\n\nSee the README for installation, worked examples and two warnings worth reading before you start: state takes IRS SOI codes rather than the FIPS codes CPS, ACS and IPUMS ship, and taxsim32 is a frozen 2022 build whose results diverge from taxsim35 for recent years.","title":"Taxsim.jl"},{"category":"section","location":"#Submitting-returns","page":"Home","text":"","title":"Submitting returns"},{"category":"section","location":"#Helpers","page":"Home","text":"","title":"Helpers"},{"category":"section","location":"#Errors","page":"Home","text":"","title":"Errors"},{"category":"function","location":"#Taxsim.taxsim35","page":"Home","text":"taxsim35(df; kwargs...)\n\nCompute federal and state income tax liabilities with TAXSIM 35, NBER's current release.\n\ndf may be a DataFrame or any other Tables.jl source with at least one row, whose columns are named exactly as in NBER's TAXSIM 35 variable list (45 names; order does not matter). Unsupplied variables are treated as zero by the server. missing values are rejected, naming the offending rows.\n\nwarning: state uses SOI codes, not FIPS\nTAXSIM numbers states 1 Alabama, 2 Alaska, 3 Arizona, 4 Arkansas, 5 California, … CPS, ACS and IPUMS ship FIPS codes, which diverge from Arizona onward. Passing FIPS produces no error and the wrong state's tax — convert with fips_to_taxsim.\n\nFederal law is coded for 1960–2023 and state law from 1977, with 2022 onward extrapolated from 2021 at an assumed 2.5% inflation.\n\nKeyword Arguments\n\nfull: request the full set of TAXSIM return variables. Defaults to false.\nlong_names: label the return variables with their long TAXSIM names. Defaults to false.\nmtr: the margin the returned marginal tax rates are computed with respect to. One of :taxpayer (default), :secondary, :wages, :dividends, :interest, :other_income, :mortgage, :deductions, :ltcg or :none. May also be a vector, one entry per row, since TAXSIM accepts mtr per record. Note :none (NBER's \"Don't bother\" code 0) was measured to return the same rates as the default rather than skipping the calculation, so do not rely on it to speed up large jobs.\nconnection: :ssh (default) tries both NBER hosts on ports 22 and 443 and falls back to HTTP with a warning if all fail; :ssh_only disables that fallback; :http posts to NBER's CGI endpoint, which needs no ssh binary but is reported to fail above ~5,000 records.\ntimeout: seconds before the transfer is abandoned. Defaults to 600.\nmissing_action: :error (default) rejects missing values, naming the offending rows; :zero sends zeros instead, which is how TAXSIM treats absent inputs anyway.\nchecks: validate the input before submitting. Defaults to true.\n\nOutput\n\nA DataFrame of the requested TAXSIM return variables, in the same row order as the input, so hcat(df, out, makeunique=true) merges input and output. The TAXSIM version and transport used are attached as DataFrame metadata.\n\nExamples\n\nusing DataFrames, Taxsim\n\ndf = DataFrame(year = 1980, mstat = 2, ltcg = 100_000)\ntaxsim35(df)\n\n# marginal rate with respect to the secondary earner, full detail\ntaxsim35(df, full = true, mtr = :secondary)\n\n\n\n\n\n","title":"Taxsim.taxsim35"},{"category":"function","location":"#Taxsim.taxsim32","page":"Home","text":"taxsim32(df; kwargs...)\n\nCompute federal and state income tax liabilities with TAXSIM 32.\n\nwarning: TAXSIM 32 is a frozen 2022 build\nThe hosted TAXSIM 32 calculator is dated 11/03/22 and its state tax law is coded only through 2020. It diverges materially from TAXSIM 35 for 2022 and 2023 — on one test record (married joint, California, $200,000 of wages) 2023 federal tax differs by over $12,000 and the 2022 state marginal rate by 3.3 points.taxsim32 exists so that work published against this endpoint stays reproducible. For new work use taxsim35.\n\nAccepts the 36 TAXSIM 32 input variables. Otherwise identical to taxsim35.\n\nKeyword Arguments\n\nfull: request the full set of TAXSIM return variables. Defaults to false.\nlong_names: label the return variables with their long TAXSIM names. Defaults to false.\nmtr: the margin the returned marginal tax rates are computed with respect to. One of :taxpayer (default), :secondary, :wages, :dividends, :interest, :other_income, :mortgage, :deductions, :ltcg or :none. May also be a vector, one entry per row, since TAXSIM accepts mtr per record. Note :none (NBER's \"Don't bother\" code 0) was measured to return the same rates as the default rather than skipping the calculation, so do not rely on it to speed up large jobs.\nconnection: :ssh (default) tries both NBER hosts on ports 22 and 443 and falls back to HTTP with a warning if all fail; :ssh_only disables that fallback; :http posts to NBER's CGI endpoint, which needs no ssh binary but is reported to fail above ~5,000 records.\ntimeout: seconds before the transfer is abandoned. Defaults to 600.\nmissing_action: :error (default) rejects missing values, naming the offending rows; :zero sends zeros instead, which is how TAXSIM treats absent inputs anyway.\nchecks: validate the input before submitting. Defaults to true.\n\nOutput\n\nA DataFrame of the requested TAXSIM return variables, in the same row order as the input, so hcat(df, out, makeunique=true) merges input and output. The TAXSIM version and transport used are attached as DataFrame metadata.\n\n\n\n\n\n","title":"Taxsim.taxsim32"},{"category":"function","location":"#Taxsim.fips_to_taxsim","page":"Home","text":"fips_to_taxsim(fips)\n\nConvert FIPS state codes — what CPS, ACS and IPUMS extracts contain — to the SOI codes TAXSIM expects. Accepts a number or any iterable; 0 and missing pass through unchanged.\n\nThe two schemes agree only for Alabama and Alaska: FIPS 6 is California but SOI 6 is Colorado, so passing FIPS codes directly to state produces no error and the wrong state's tax.\n\njulia> fips_to_taxsim(6)          # FIPS California is SOI California\n5\n\njulia> fips_to_taxsim(6) == 6     # ... but not SOI 6, which is Colorado\nfalse\n\njulia> fips_to_taxsim([1, 4, 6])\n3-element Vector{Int64}:\n 1\n 3\n 5\n\nIn practice:\n\ndf.state = fips_to_taxsim(df.statefip)\n\n\n\n\n\n","title":"Taxsim.fips_to_taxsim"},{"category":"function","location":"#Taxsim.taxsim_server_version","page":"Home","text":"taxsim_server_version(version = 35; connection = :ssh, timeout = 60) -> String\n\nAsk the server which build it is running, e.g. \"NBER TAXSIM Model v35 (03/22/24) With TCJA\".\n\nNBER runs several builds concurrently and rebuilds them without changing the version number, so a replication package should record this alongside its results.\n\n\n\n\n\n","title":"Taxsim.taxsim_server_version"},{"category":"type","location":"#Taxsim.TaxsimError","page":"Home","text":"TaxsimError\n\nSupertype for every error this package raises deliberately. Callers looping over survey years can retry a TaxsimTransportError while letting a TaxsimInputError through, which was impossible when everything was an ErrorException.\n\n\n\n\n\n","title":"Taxsim.TaxsimError"},{"category":"type","location":"#Taxsim.TaxsimInputError","page":"Home","text":"TaxsimInputError\n\nThe submitted data frame is not something TAXSIM will accept. Not worth retrying.\n\n\n\n\n\n","title":"Taxsim.TaxsimInputError"},{"category":"type","location":"#Taxsim.TaxsimTransportError","page":"Home","text":"TaxsimTransportError\n\nThe NBER server could not be reached, or the connection failed mid-transfer. Worth retrying.\n\n\n\n\n\n","title":"Taxsim.TaxsimTransportError"},{"category":"type","location":"#Taxsim.TaxsimServerError","page":"Home","text":"TaxsimServerError\n\nTAXSIM itself rejected the submission. Carries the server's own message text. Not worth retrying without changing the input.\n\n\n\n\n\n","title":"Taxsim.TaxsimServerError"}]
}
